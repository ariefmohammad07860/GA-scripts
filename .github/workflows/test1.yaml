name: Arief Mohammad workflow

on: push
jobs:
   build:
      runs-on: ubuntu-latest
      steps:
        - name: Welcome message
        - run: echo "This is Arief"

        - name: Display Date Command
        - run: date
        
