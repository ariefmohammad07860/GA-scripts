name: Concurrency workflow
on:
   workflow_dispatch
concurrency:
   group: main-branch-group
   cancel-in-progress: false
jobs:
   demo:
      runs-on: ubuntu-latest
      steps: 
         - run: echo "Started..."
         - run: sleep 300
         - run: echo "Finshed"
